// ── struct (common.h의 FrameUniforms로 생성됨) ────────────────
struct FrameUniforms {
    projectionViewModel: mat4x4,
}

// ── host-side 진입점 ──────────────────────────────────────────
@backend(metal)
fn main() {
    // salvation이 자동으로 Metal 보일러플레이트를 생성해 실행합니다
}

// ── vertex shader ─────────────────────────────────────────────
@backend(metal) @vertex
fn vert(position: float4, color: float4) -> float4 {
    return position;
}

// ── fragment shader ───────────────────────────────────────────
@backend(metal) @fragment
fn frag(color: float4) -> float4 {
    return color;
}
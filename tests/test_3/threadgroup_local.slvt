kernel fn prefix_sum(
    input:  device [f32],
    output: device mut [f32],
) {
    let shared: threadgroup mut [f32; 32];
    let i = thread.x;
    shared[i] = input[i];
    barrier();
    let val = simd_sum(shared[i]);
    output[i] = val;
}